import React, { Component } from 'react';
import TimelineWrapper from '../containers/TimelineWrapper';

export default class App extends Component {
  render() {
    return (
      <div>
        <TimelineWrapper />
      </div>
    );
  }
}
